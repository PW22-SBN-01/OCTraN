version: "3.9"
services:
  dev:
    # Will build ./docker/Dockerfile-GPU
    # This Dockerfile is for GPU based development
    build:
      context: .
      dockerfile: ./docker/Dockerfile-GPU
    volumes:
      - ./:/OCTraN
      - ~/.cache:/root/.cache
      - ~/.torch:/root/.torch
      - ~/.config:/root/.config
      - ~/.bash_history:/root/.bash_history
      - ~/.netrc:/root/.netrc
      - ~/Datasets/SemanticKITTI/dataset/:/OCTraN/dataset/semantic_kitti/data_odometry_voxels/dataset
      - ~/Datasets/SemanticKITTI/dataset_preprocess/:/OCTraN/dataset/semantic_kitti/preprocess_new
      
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0']
              capabilities: [gpu]
